'use strict';

var projects = [];

// projects need a title, what language they are, the url and small description
function Projects (projs) {
  this.title = projs.title;
  this.language = projs.language;
  this.projectUrl = projs.projectUrl
}
